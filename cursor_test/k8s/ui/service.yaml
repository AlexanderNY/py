apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: app
  labels:
    app: ui
spec:
  selector:
    app: ui
  ports:
    - port: 8100
      targetPort: 8100
      name: http
  type: ClusterIP
