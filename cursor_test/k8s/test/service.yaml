apiVersion: v1
kind: Service
metadata:
  name: test
  namespace: app
  labels:
    app: test
spec:
  selector:
    app: test
  ports:
    - port: 8008
      targetPort: 8008
      name: http
  type: ClusterIP
