apiVersion: v1
kind: Service
metadata:
  name: scheduler
  namespace: app
  labels:
    app: scheduler
spec:
  selector:
    app: scheduler
  ports:
    - port: 8003
      targetPort: 8003
      name: http
  type: ClusterIP
