# todo здесь собираем список долгосрочных задач
Настройка: в приложении VK указать Redirect URI ровно как в VK_OAUTH_REDIRECT_URI (например http://localhost:8000/vk/oauth/callback, если шлюз на порту 8000 без префикса /api — как у Threads). В .env задать VK_APP_ID, VK_APP_SECRET, VK_OAUTH_REDIRECT_URI и согласованный с ними URL в кабинете VK.

проверить что ТГ каналы доступны и вы подписчик при сохранении

как дзен? rss? 