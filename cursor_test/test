1 полная интеграция с тг-вычитка по фильтру из нескольких каналов, затем публикация в один канал
2 все вычитанные сообщения в одну таблицу, затем на обработку, затем на публикацию

Таблица постс должна быть редактируемой

В таблице постс надо добавить источник, например канал и тип (тг)

Сделать для супервизора видимость всех постов и всех конфигураций сразу

Статус передано в обработку
Обработчик по умолчанию пусть убирает лишние теги и очищает от смайликов

Далее если галка на ревью-ждать пока пользователь скорректирует

Все что готово, отправлять в постс_сервисов

Взять в приоритет тг и вк

Тарифы и роли сделать возможным назначить роли из админки